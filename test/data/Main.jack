class Main {
    // Something here
    field int x;
    field char y;
    static bool help;

    constructor Main new (int i) {
        do hello(2);
        return this;
    }

    function void main(int p1, boolean p2) {
        var Array a;
        var int length;
        var int i, sum;

        let length = Keyboard.readInt("How many numbers? ");
        let a = Array.new(length); // constructs the array

        let i = 0;
        while (i < length) {
           let a[i] = Keyboard.readInt("Enter a number: ");
           let sum = sum + a[i];
           let i = i + 1;
        }

        if(i = 1) {
            let i = 1;
            let i = 3;
        } else {
            let i = 3;
            let i = 3;
        }

        do Output.printString("The average is ");
        do Output.printInt(sum / length);
        let i = 1;
    }

    method int hello(int a) {
        return 1;
    }
}
